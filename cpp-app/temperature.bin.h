#pragma once

#define TEMPERATURE_MAP_COUNT (1990)
#define TEMPERATURE_MAP_WIDTH (330)
#define TEMPERATURE_MAP_HEIGHT (235)

#include <vector>

void loadTemperaturePixels(std::vector<char>& temperaturePixels);